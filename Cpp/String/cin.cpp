// Toán tử rút trích (Extraction operator) >>

cin >> variable_1 >> variable_2 >> ... ;

// Insert section (Toán tử them vào) <<

cout << variable_1 << variable_2 << ... ;

// Ký tự khoảng trắng (white-space character) -> kí tự trống, dấu tab, xuống hàng

#include<iostream>
using namespace std;

int main(){
    string s1, s2;

    cin >> s1 >> s2;

    cout << s1 << endl << s2 << endl;

    return 0;
}